apiVersion: v1
kind: Pod
metadata:
  name: dummy-pod
spec:
  containers:
  - name: dummy
    image: busybox
    command: ["sleep", "3600"]
    volumeMounts:
    - name: fabric-data
      mountPath: /fabric
  volumes:
  - name: fabric-data
    persistentVolumeClaim:
      claimName: fabric-pvc
